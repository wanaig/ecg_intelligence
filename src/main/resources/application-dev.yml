server:
  port: 8080

spring:
  datasource:
    url: '${MYSQL_URL:jdbc:mysql://112.124.70.235:4406/ecg_test2?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true&useSSL=false}'
    username: ${MYSQL_USERNAME:root}
    password: ${MYSQL_PASSWORD:wanaiwanai}
    driver-class-name: com.mysql.cj.jdbc.Driver
  sql:
    init:
      mode: never

mybatis:
  mapper-locations: classpath*:mapper/**/*.xml
  type-aliases-package: com.hnkjzy
  configuration:
    map-underscore-to-camel-case: true

logging:
  level:
    com.hnkjzy: info
